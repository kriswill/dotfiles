home/podman-desktop/.local/share/containers/podman-desktop/configuration/settings.json filter=normalize-podman-settings

# Shell scripts can contain literal ANSI/control bytes (\033, \x1f, etc.)
# that trip git's binary heuristic — force textual diff handling.
scripts/*.sh text diff
