---------------------
---- MY PROGRAMS ----
---------------------

-- Set programs that you use. Shared across modules — required by keybindings.lua
-- (`local programs = require("programs")`), so the binds below stay in sync with
-- a single edit here.
return {
  terminal = "ghostty",
  fileManager = "dolphin",
  menu = "fuzzel",
}
