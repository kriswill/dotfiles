# Kris' ssh config — stow-managed (home/ssh/.ssh/config), symlinked to
# ~/.ssh/config by modules/darwin/dotfiles-stow.nix. 
# Host-specific entries are pulled in from
# ~/.ssh/config.d/* (e.g. sops-managed internal hosts).
Include /Users/k/.ssh/config.d/*

Host *
  ForwardAgent yes

  IdentityAgent "~/Library/Group Containers/2BUA8C4S2C.com.1password/t/agent.sock"
