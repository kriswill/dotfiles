# Route every python-keyring consumer (Gajim is the one that matters here)
# to the 1Password-backed backend in ./backends — no Secret Service daemon.
[backend]
default-keyring=op_keyring.OnePasswordKeyring
keyring-path=/home/k/.config/python_keyring/backends
