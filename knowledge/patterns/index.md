# patterns

Named architectural patterns this repository is built on. Each concept
explains one load-bearing idea — what it is, why it's shaped that way, and
where it's implemented.

## Concepts

* [Dendritic Module Layout](dendritic-modules.md) - Every .nix file under modules/ is auto-discovered as a flake-parts module via import-tree — no manual import lists anywhere.
* [Module Option Pattern](module-option-pattern.md) - Feature modules expose kriswill.<feature>.enable (or programs.<name> via mkProgramOption) and gate all config behind lib.mkIf.
* [Store-path-embedding Configs](store-path-configs.md) - Config files that must reference /nix/store paths are generated by their feature's darwin module and linked during activation, not stowed.
* [Stow Tree](stow-tree.md) - Plain config files live in home/ as GNU Stow packages symlinked into $HOME, pointing at the live repo so edits apply without a rebuild.
* [Sub-flake Extraction](subflake-extraction.md) - Packages destined for standalone life live under flakes/<name>/ as self-contained flakes consumed via relative-path inputs — extraction to a separate repo is a one-line URL swap.
