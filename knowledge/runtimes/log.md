# Log

## 2026-08-02

- **Creation** — bundle established: [bun](bun.md) moved in from the bundle
  root (formerly `bun-runtime.md`; frontmatter `type` retyped `Reference` →
  `Runtime`); inbound links repointed.
- **Creation** — [nix](nix.md): new concept for Nix's executable half —
  evaluator, `/nix/store`, `nix-daemon`; Determinate Nix on every host, the
  one-daemon-per-machine contract — twinned both ways with
  [Nix Language](../languages/nix.md).
