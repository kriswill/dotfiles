diff --git a/src/cli.js b/src/cli.js
index e899e21..c707442 100644
--- a/src/cli.js
+++ b/src/cli.js
@@ -22,7 +22,7 @@ import {
 } from "./reasonix-config.js";

 const __dirname = path.dirname(fileURLToPath(import.meta.url));
-const VERSION = JSON.parse(fs.readFileSync(path.join(__dirname, "..", "package.json"), "utf8")).version;
+const VERSION = "1.1.2";

 const HELP = `ccglass v${VERSION} — see what your coding agent sends to the model

@@ -158,7 +158,7 @@ function mcpArgs(opts) {
     mcpServers: {
       ccglass: {
         command: process.execPath,
-        args: [path.join(__dirname, "mcp.js")],
+        args: ["__mcp__"],
         env: {
           CCGLASS_ROOT: opts.dir,
           CCGLASS_CWD: process.cwd(),
@@ -429,6 +429,12 @@ async function view(opts) {
 export { exportEntry, migrate, repack, rmCmd, usageCmd } from "./log-cli.js";

 export async function main(argv) {
+  // Compiled single-binary mode: re-exec'd as `ccglass __mcp__` to serve the MCP
+  // stdio server (see mcpArgs). mcp.js starts its server as a load side-effect.
+  if (argv[0] === "__mcp__") {
+    await import("./mcp.js");
+    return;
+  }
   const { opts, rest } = parseArgs(argv);
   const cwd = process.cwd();

diff --git a/src/server.js b/src/server.js
index 50880ea..2a39abb 100644
--- a/src/server.js
+++ b/src/server.js
@@ -12,9 +12,23 @@ import { aggregateSessionStats, latencyMs, requestTiming, sessionModels } from "
 import { diffBlockLists } from "./diff.js";
 import { renderExport } from "./export.js";
 import { summarizeUsage } from "./usage.js";
-
-const __dirname = path.dirname(fileURLToPath(import.meta.url));
-const WEB_DIR = path.join(__dirname, "..", "web");
+// Embed the dashboard assets so `bun build --compile` bakes them into the binary;
+// the script-relative WEB_DIR read below does not survive compilation.
+import webIndexHtml from "../web/index.html" with { type: "file" };
+import webAppJs from "../web/app.js" with { type: "file" };
+import webThemeJs from "../web/theme.js" with { type: "file" };
+import webStyleCss from "../web/style.css" with { type: "file" };
+import webStreamCss from "../web/stream.css" with { type: "file" };
+
+const WEB_FILES = {
+  "index.html": webIndexHtml,
+  "app.js": webAppJs,
+  "theme.js": webThemeJs,
+  "style.css": webStyleCss,
+  "stream.css": webStreamCss,
+};
+const WEB_CACHE = {};
+for (const [n, fp] of Object.entries(WEB_FILES)) WEB_CACHE[n] = Buffer.from(await Bun.file(fp).arrayBuffer());

 const MIME = { ".html": "text/html", ".js": "text/javascript", ".css": "text/css" };

@@ -148,13 +162,13 @@ function stream(res, clients) {

 function serveStatic(p, res) {
   const file = p === "/" ? "index.html" : p.replace(/^\//, "");
-  const full = path.join(WEB_DIR, file);
-  if (!full.startsWith(WEB_DIR) || !fs.existsSync(full)) {
+  const buf = WEB_CACHE[file];
+  if (!buf) {
     res.writeHead(404).end("not found");
     return;
   }
-  res.writeHead(200, { "content-type": MIME[path.extname(full)] || "application/octet-stream" });
-  fs.createReadStream(full).pipe(res);
+  res.writeHead(200, { "content-type": MIME[path.extname(file)] || "application/octet-stream" });
+  res.end(buf);
 }

 function attach(id, ext) {
